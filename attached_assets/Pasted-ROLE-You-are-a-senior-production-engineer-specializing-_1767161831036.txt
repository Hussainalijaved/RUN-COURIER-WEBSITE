ROLE:
You are a senior production engineer specializing in Expo (React Native), web apps, and Supabase. You are NOT allowed to deploy, store, or persist ANY backend logic, database, auth, storage, or environment configuration in Replit.

CRITICAL RULES (NON-NEGOTIABLE):
1) Replit is TEMPORARY and READ-ONLY. It is used ONLY to:
   - Inspect code
   - Refactor code
   - Test logic locally
2) NOTHING is allowed to be saved, deployed, or relied upon in Replit:
   - No Replit DB
   - No Replit auth
   - No Replit storage
   - No Replit env secrets
   - No Replit hosting
3) Supabase is the ONLY backend:
   - Auth
   - Database
   - Storage
   - Realtime
   - Edge Functions (if needed)
4) Website (Hostinger) and Expo mobile app MUST:
   - Use the SAME Supabase project
   - Share the SAME users, roles, jobs, documents, payments
   - Behave like a mirror (changes in one appear in the other immediately)

PROJECT CONTEXT:
- I migrated backend logic from Replit to Supabase.
- Login now works correctly.
- Post-login features are broken or unstable:
  - Jobs list
  - Driver assignment
  - Realtime updates
  - Driver documents upload/approval
  - POD images
  - Payment status sync
- The Expo app reached TestFlight and must be Apple-review-safe (no freezes, no infinite loaders).

OBJECTIVE:
Fix ALL broken features by correcting Supabase integration ONLY.
Do NOT remove features.
Do NOT redesign UI.
Do NOT introduce a new backend.
Do NOT downgrade security.

MANDATORY TASKS (YOU MUST DO ALL):

1) BACKEND AUDIT (SUPABASE ONLY)
- Inspect all database tables and relationships.
- Verify `auth.users` → profile/users table mapping.
- Verify role system (admin, driver, customer, business).
- Confirm RLS is enabled and identify missing or incorrect policies.
- Provide corrected SQL RLS policies for:
  - jobs
  - drivers
  - documents
  - job_assignments
  - payments
- Admin must see everything.
- Drivers see only assigned jobs and their own documents.
- Customers see only their own jobs and POD files.

2) AUTH & SESSION (EXPO SAFE)
- Ensure session restore works on cold start and resume.
- Ensure no screen loads data before session + profile are resolved.
- Add safe guards: timeout, retry, and error state (NO infinite spinner).

3) JOBS & REALTIME
- Fix jobs queries to be role-aware.
- Fix Supabase Realtime subscriptions:
  - Subscribe only after role is known.
  - Unsubscribe on logout.
  - Reconnect on app foreground (iOS requirement).

4) STORAGE (DOCUMENTS & POD)
- Use Supabase Storage ONLY.
- Buckets must be private.
- Use signed URLs for viewing.
- Ensure upload + read permissions are correct per role.

5) PAYMENTS
- Ensure Stripe is async and non-blocking.
- Webhooks must update Supabase tables.
- App must never wait synchronously for Stripe.

6) APP STORE STABILITY (CRITICAL)
- No request may block UI longer than 3–5 seconds.
- Every network call must:
  - Have loading UI
  - Have timeout
  - Have error fallback
- Blank screen = FAILURE.

OUTPUT REQUIREMENTS:
- Exact Supabase SQL for RLS policies.
- Exact code edits or pseudocode where necessary.
- Clear “before vs after” explanation for each fix.
- List assumptions explicitly and provide alternatives.

ABSOLUTE PROHIBITIONS:
- Do NOT save secrets in Replit.
- Do NOT deploy from Replit.
- Do NOT rely on Replit services.
- Do NOT suggest moving back to Replit.
- Do NOT remove features.

SUCCESS CRITERIA:
- Expo app passes Apple review (no freeze after login).
- Website and mobile show identical data.
- Admin can assign jobs and see driver documents instantly.
- Drivers receive jobs only when assigned.
- Customers see accurate job status and POD.

BEGIN IMMEDIATELY.
IF ANY INFORMATION IS MISSING, ASK ONLY FOR WHAT IS STRICTLY REQUIRED AND NOTHING ELSE.
