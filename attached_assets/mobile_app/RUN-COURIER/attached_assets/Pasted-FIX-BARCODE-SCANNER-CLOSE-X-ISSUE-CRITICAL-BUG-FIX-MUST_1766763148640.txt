FIX BARCODE SCANNER CLOSE (X) ISSUE

CRITICAL BUG FIX — MUST BE DONE PROPERLY

You are working on a React Native + Expo courier driver app (Run Courier).

🎯 PROBLEM

In the Scan Pickup Barcode screen (camera scanner modal/screen), the Close (X) button sometimes does not respond and the driver gets stuck in the scanner.

This is unacceptable for a production courier app.

🚫 NOT ACCEPTABLE

❌ Scanner stays open after tapping X

❌ Camera continues running in background

❌ Only one way to exit scanner

❌ Navigation stack broken or frozen

✅ REQUIRED FIX (FOLLOW EXACTLY)
1️⃣ SCANNER MUST BE A CONTROLLED MODAL / SCREEN

Ensure the barcode scanner is implemented as one of the following (choose one and do it correctly):

A controlled Modal

OR a dedicated navigation screen

It MUST NOT be an uncontrolled overlay.

2️⃣ CLOSE (X) BUTTON — HARD REQUIREMENTS

When user taps X:

✅ Scanner immediately closes

✅ Camera is fully stopped

✅ Barcode scanning listener is unmounted

✅ App returns to previous screen safely

No delay. No animation blocking.

3️⃣ CAMERA LIFECYCLE (IMPORTANT)

You MUST explicitly handle camera lifecycle:

On OPEN:

Request camera permission (if not granted)

Start camera + barcode scanner

On CLOSE (X):

Stop camera

Disable barcode scanning

Unmount scanner component

Clear any scanning state

⚠️ Do NOT rely on automatic unmount only.

4️⃣ MULTIPLE EXIT METHODS (MANDATORY)

Driver must be able to exit scanner using ALL of these:

❌ Tap X button

🔙 Android back button

⬇️ iOS swipe down (if modal)

🔄 Navigation goBack()

All must work consistently.

5️⃣ PREVENT SCAN LOCK

Implement a scan lock guard:

Once a barcode is detected:

Temporarily disable scanning

Process result

Close scanner OR allow manual close

This prevents scanner freezing due to multiple detections.

6️⃣ Z-INDEX / TOUCH ISSUE FIX

Ensure:

Close (X) button is above camera view

pointerEvents="auto"

Correct zIndex / elevation

Button is NOT blocked by camera overlay

This is a common cause of “X not clickable”.

7️⃣ FILES TO REVIEW / FIX

You MUST inspect and fix logic in:

ScanBarcodeModal.tsx OR ScanBarcodeScreen.tsx

Camera component (expo-camera or expo-barcode-scanner)

Navigation logic

Refactor if needed — correctness > speed.

8️⃣ UX REQUIREMENTS

Close button must be large & tappable

Respect safe area (not under notch)

No accidental tap blocking

9️⃣ FINAL VALIDATION (MANDATORY)

Before finishing, confirm ALL of the following:

✅ X button always closes scanner

✅ Camera turns OFF on close

✅ No frozen state

✅ No memory leak

✅ Driver never gets stuck